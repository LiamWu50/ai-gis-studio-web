import type { UserLayer } from "@/features/layers/layer-workspace";

export const saveUserLayer = async (layer: UserLayer) => {
  // TODO: Replace with the user layer tree persistence API when it is ready.
  return layer;
};
